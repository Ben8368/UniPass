# 扩展安全经验

- E-001：为隐藏空密码账号，只向 Popup 返回可用性状态；不要用批量返回明文密码实现列表过滤。
- E-002：目录同步的单应用失败必须保留为“不完整”状态，不能覆盖最后一次完整缓存。
- E-003：获取凭据前和注入前分别检查页面；只允许 HTTPS、匹配 origin/path 且仍为活动标签页。
- E-004：账号、token、版本和目录缓存的身份范围与 TTL 必须明确；未知错误不能缓存成业务事实。
- E-005：Service Worker、Popup 和 Content Script 的消息字段变化要同步契约、调用端和回归测试。
